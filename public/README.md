Place your resume PDF here as `Sanjay-Krishnan.pdf` so the site can serve it at `/Sanjay-Krishnan.pdf`.

Steps:
1. Ensure `public/Sanjay-Krishnan.pdf` exists (it's already present).
2. Start the dev server: `npm run dev` (or `yarn dev`).
3. Use the "View Resume" button in the hero section to preview it and "Download Resume" to save it.

If you want a different filename or path, update the `/Sanjay-Krishnan.pdf` paths in `src/components/Hero.tsx` accordingly.